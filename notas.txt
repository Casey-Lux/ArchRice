# NOTAS

## Paquetes:

### Entorno gráfico básico (done)
xorg-server
xorg-xinit
xorg-xrandr
startx -> Al parecer viene incluido cuando instalo xorg 

### Qtile (done)
qtile
python-psutil
python-setuptools

## Arranque con qtile (done)

### Edita ~/.xinitrc
* 'exec qtile start' -> Añade esa linea al final

### Ejecuta (done)
startx
win + Enter -> para abrir una terminal
win + ctrl + q -> para salir de qtile y volver a la terminal

### Paquetes necesarios para mi configuración de qtile (done)
picom 
feh 
dmenu 
lightdm -> inicio gráfico, se puede eliminar / descartar 
alacritty 
screenfetch -> Puro lujo, nada más

### Paquetes de utilidad (done)
git -> Ya debería estar para clonar el repositorio
neovim

### Configuración de qtile (done)
git clone https://github.com/Casey-Lux/ArchRice
* Será necesario copiar los archivos de configuración a sus directorios
* Es necesario que autostart.sh sea ejecutable
* Es necesario incluir el directorio de wallpapers
* Es necesario instalar las fuentes
$ sudo pacman -Sy ttf-ubuntu-mono-nerd ttf-nerd-fonts-symbols ttf-fira-code
	
### Paquetes para el sistema (done)
firefox -> navegador
thunar -> explorador de archivos gráfico
scrot -> capturas de pantalla

*** Antes será necesario arreglar el problema del foco (done)
* Agregar una configuración correcta para picom (done)

### Paquetes para la terminal
zsh:
sudo pacman -S --noconfirm zsh
chsh -s /usr/bin/zsh "$USER"

ohmyzsh:
sudo pacman -S curl
sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"


tmux:
sudo pacman -S tmux
tmux
* Copiar el archivo de configuración de tmux (done)

btop -> alige un tema desde las opciones
lf:
sudo pacman -S lf bat poppler


* Es necesario activar las opciones de copiado y pegado en la vm (falló el copiar y pegar)

* Instalación de powerlevel10k con instant prompt (done)




